# Backend proxy configuration

# Real LLM API keys (kept on backend only, never exposed to frontend)
ANTHROPIC_API_KEY=sk-ant-xxx
OPENAI_API_KEY=sk-xxx
DEEPSEEK_API_KEY=sk-xxx

# Server config
PORT=3000
