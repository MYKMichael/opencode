{
  "name": "opencode-backend-proxy",
  "version": "1.0.0",
  "description": "Backend LLM proxy for OpenCode - manages API keys and tracks token usage",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
